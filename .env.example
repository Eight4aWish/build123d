# Optional: force ocp_vscode to connect to the VS Code OCP CAD Viewer backend.
# Defaults are usually fine; only set these if you need to override.

# OCP_VSCODE_HOST=127.0.0.1
# OCP_VSCODE_PORT=3939
